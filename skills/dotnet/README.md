# .NET Skills

C#/ASP.NET Core web API conventions for inherited and green services.

## Model-invoked

Model- or user-reachable via skill name and trigger phrasing.

- **[z-dotnet-core](./z-dotnet-core/SKILL.md)** — C#/ASP.NET Core web APIs for inherited and green services — minimal APIs, nullable reference types as errors, constructor-injection DI, EF Core AsNoTracking reads, async all the way. Current LTS is .NET 10.
