# Lisp Skills

Embeddable Scheme-flavored Lisp VM and compiler design for host applications.

## Model-invoked

Model- or user-reachable via skill name and trigger phrasing.

- **[z-lisp-core](./z-lisp-core/SKILL.md)** — Embeddable Scheme-flavored Lisp interpreters/compilers embedded in a host app — lexical closures, proper tail calls as a requirement, hygienic macros by default, call/cc only if needed, tagged-value VM/host boundary.
