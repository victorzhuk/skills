---
"victorzhuk-skills": minor
---

Two new Go skills: `z-go-stdlib` (stdlib-first discovery — `go doc` lookup workflow, task→package coverage map through Go 1.27, commonly-reinvented-wheels table, dependency gate) and `z-go-idioms` (official-canon design idioms — Effective Go, Go Proverbs, Code Review Comments, Google's style guide — as decision rules, with a router table delegating mechanics to sibling skills). `z-go-modernize` updated for Go 1.27: `encoding/json/v2` promoted to stdlib, `strings.CutLast`/`bytes.CutLast`, std `uuid` package, `url.URL.Clone`, `go doc package@version`. Nine catalog descriptions compressed to keep the aggregate under the 40k budget.
